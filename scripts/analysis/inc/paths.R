CORPUS <- path(params$base_dir, "corpus.csv")

IMPLICIT_DECLARATIONS <- path(params$base_dir, "implicit-declarations.csv")
IMPLICIT_CALLSITES <- path(params$base_dir, "implicit-callsites.csv")
IMPLICITS_EXCEPTIONS <- path(params$base_dir, "implicits-exceptions.csv")

REPO_METADATA <- path(params$base_dir, "repo-metadata.csv")
PROJECTS_GH_INFO_PINNED <- path(params$base_dir, "projects-github-info.csv.pinned")
PROJECTS_GH_INFO <- path(params$base_dir, "projects-github-info.csv")

ALL_PROJECTS_FILE <- path(params$base_dir, "all-projects.txt")
ALL_PROJECTS_DIR <- path(params$base_dir, "all-projects")

SBT_PROJECTS_FILE <- path(params$base_dir, "sbt-projects.txt")
SO_PROJECTS_FILE <- path(params$base_dir, "so-projects.txt")
PROJECTS_FILE <- path(params$base_dir, "projects.txt")
PROJECTS_DIR <- path(params$base_dir, "all-projects")
